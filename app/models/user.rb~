class User < ActiveRecord::Base

  #Validations
  #
  
  validates_presence_of [ :last_name, :first_name, :email ], :message =>"is required"
  validates_uniqueness_of :email
  validates_format_of :email, :with => /\A([^@\s]+)@((?:[-a-z0-9]+\.)+[a-z]{2,})\Z/i, :on => :create
  
end
